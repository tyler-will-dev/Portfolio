# Simple Read Me file #
-----------------------

## Commands

npm-start: Starts the development server

git add .: Stages the changes

git commit -m: Creates a local commit with a message

git push origin main: Pushes to git repo on main branch